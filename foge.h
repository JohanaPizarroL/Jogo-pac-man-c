#ifndef FOGE_H
    #define FOGE_H

    #define CIMA 'w'
    #define BAIXO 's'
    #define ESQUERDA 'a'
    #define DIREITA 'd'

    int acabou();
    void move(char direcao);
    int direcao_valida(char direcao);
#endif